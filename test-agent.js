#!/usr/bin/env node
import { agent } from './app/lib/agent/index.js';

console.log('\n🎵 Testing HarmonyAI Agent...\n');

// Just test to see if the agent can be imported and the paths are resolved correctly
process.exit(0);
